# Thien-Tam
Ứng dụng quản lý nhà thuốc


new_kh.addActionListener(new ActionListener() {

    @Override

    public void actionPerformed(ActionEvent e) {

        // Tạo một đối tượng mới của cửa sổ đăng ký

        signup_GUI signupWindow = new signup_GUI();

        

        // Cửa sổ đăng ký sẽ tự hiển thị vì bạn đã có this.setVisible(true) trong constructor

    }

});